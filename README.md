# Backend

FastAPI service for dual-channel TIFF validation, tiled analysis, Qwen-based layer decomposition, metric computation, and ROI profiling.

Run locally with:

```bash
uvicorn app.main:app --reload
```

